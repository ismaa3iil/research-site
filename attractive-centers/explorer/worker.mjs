import {normalize,triangle,heart,four,fourCombined,tetra,area,hull,clipAll,polyHP} from './geometry.mjs';
export function compute({mode,points,on,count=49}){const {p,g,s}=normalize(points);let d,result;if(mode==='triangle'){d=triangle(p);if(d.degenerate)throw Error('The triangle is collinear. Move a vertex away from the line.');let inner=heart(p,count);result={...d,heart:inner,hull:hull(p),baseArea:area(p)};}else if(mode==='four'){if(area(hull(p))<1e-10)throw Error('The four points are collinear. Move a vertex away from the line.');d=four(p);result={...d,combined:fourCombined(d,on),baseArea:area(d.hull)};}else{d=tetra(p,on);if(d.degenerate||!d.base||d.base.volume<1e-10)throw Error('The tetrahedron is flat. Change a vertex coordinate to restore volume.');let para=tetra(p,{para:true,bisector:false}),bi=tetra(p,{para:false,bisector:true});result={...d,para,bi};}return {mode,result,g,s};}
if(typeof self!=='undefined')self.onmessage=({data})=>{try{self.postMessage({id:data.id,...compute(data)});}catch(e){self.postMessage({id:data.id,error:e.message});}};
